// Converter — image conversion is handled client-side (Canvas API).
// This file is kept as a placeholder; no server-side image/audio processing needed.
import { Router } from 'express'
const router = Router()
export default router
